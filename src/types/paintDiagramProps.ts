type Type = string;
type x = number;
type y = number;
type width = number;
type height = number;

export interface PaintDiagramProps {
  type: Type;
  x: x;
  y: y;
  width: width;
  height: height;
}
